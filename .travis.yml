language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-japi' ]; then cd .. && eval "mv $currentfolder generator-japi" && cd generator-japi; fi
